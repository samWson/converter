apply plugin: 'java'

jar {
  manifest {
    attributes (
      'Main-Class' : 'converter.Main',
		'Implementation-Title' : project.name,
		'Developer' :'samWson@users.noreply.github.com'
    )
  }
}
